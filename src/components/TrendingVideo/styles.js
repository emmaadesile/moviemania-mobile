import styled from "styled-components";

const PosterImage = styled.View`
  width: 100%;
  height: 300px;
`;

const Title = styled.Text`
  font-size: 30px;
  font-weight: bold;
`;

const PlayButton = styled.View`
  width: 60px;
  height: 20px;
`;

const Image = styled.Image`
  width: 100%;
  height: 100%;
`;

export { PosterImage, Title, PlayButton, Image };
